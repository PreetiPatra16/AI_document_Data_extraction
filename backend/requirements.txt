fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0
python-multipart>=0.0.6
loguru>=0.7.0
opencv-python-headless>=4.8.0
pillow>=10.0.0
pdf2image>=1.16.3
pytesseract>=0.3.10
# paddlepaddle and paddleocr should be installed, but can be optional or fallback if binaries aren't available locally.
# We will write python code that dynamically imports or wraps them gracefully.
# paddlepaddle>=2.5.0
# paddleocr>=2.7.0
python-dotenv>=1.0.0
aiofiles>=23.1.0
